// //Hide warning
// jQuery('.warning').hide();

// //Show warning Slowly
// // instead of writing jQuery, we can use $ for shortcut
// $('.warning').show('slow');

// Method chaining: can add one method after another:
$('.warning').hide().show('show');

